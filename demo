<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mixed Media Studio</title>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --bg-primary: #0a0a0b;
            --bg-secondary: #111113;
            --bg-tertiary: #1a1a1d;
            --accent: #00ff88;
            --accent-dim: #00ff8833;
            --text-primary: #ffffff;
            --text-secondary: #888899;
            --border: #2a2a2d;
            --danger: #ff4466;
        }

        body {
            font-family: 'Space Grotesk', sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* Header */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 16px 24px;
            border-bottom: 1px solid var(--border);
            background: var(--bg-secondary);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            width: 36px;
            height: 36px;
            background: linear-gradient(135deg, var(--accent), #00ccff);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 14px;
            color: var(--bg-primary);
        }

        .logo-text {
            font-size: 18px;
            font-weight: 600;
            letter-spacing: -0.5px;
        }

        .logo-text span {
            color: var(--accent);
        }

        .header-actions {
            display: flex;
            gap: 12px;
        }

        .btn {
            padding: 10px 20px;
            border-radius: 8px;
            font-family: inherit;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s;
            border: none;
        }

        .btn-secondary {
            background: var(--bg-tertiary);
            color: var(--text-primary);
            border: 1px solid var(--border);
        }

        .btn-secondary:hover {
            background: var(--border);
        }

        .btn-primary {
            background: var(--accent);
            color: var(--bg-primary);
        }

        .btn-primary:hover {
            background: #00ffaa;
            transform: translateY(-1px);
        }

        .btn-primary:disabled {
            background: var(--border);
            color: var(--text-secondary);
            cursor: not-allowed;
            transform: none;
        }

        /* Main Layout */
        .main {
            display: grid;
            grid-template-columns: 280px 1fr 320px;
            height: calc(100vh - 69px);
        }

        /* Sidebar - Effects */
        .sidebar {
            background: var(--bg-secondary);
            border-right: 1px solid var(--border);
            overflow-y: auto;
            padding: 20px;
        }

        .sidebar-section {
            margin-bottom: 24px;
        }

        .sidebar-title {
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--text-secondary);
            margin-bottom: 12px;
        }

        .effect-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 8px;
        }

        .effect-card {
            aspect-ratio: 1;
            border-radius: 12px;
            background: var(--bg-tertiary);
            border: 2px solid transparent;
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 8px;
            padding: 12px;
            position: relative;
            overflow: hidden;
        }

        .effect-card::before {
            content: '';
            position: absolute;
            inset: 0;
            opacity: 0.1;
            transition: opacity 0.2s;
        }

        .effect-card:hover {
            border-color: var(--accent);
            transform: scale(1.02);
        }

        .effect-card:hover::before {
            opacity: 0.2;
        }

        .effect-card.active {
            border-color: var(--accent);
            background: var(--accent-dim);
        }

        .effect-card.active::before {
            opacity: 0.3;
        }

        .effect-icon {
            font-size: 24px;
        }

        .effect-name {
            font-size: 11px;
            font-weight: 500;
            text-align: center;
            color: var(--text-secondary);
        }

        .effect-card.active .effect-name {
            color: var(--accent);
        }

        /* Canvas Area */
        .canvas-area {
            background: var(--bg-primary);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 24px;
            position: relative;
        }

        .canvas-container {
            position: relative;
            max-width: 100%;
            max-height: calc(100vh - 200px);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
        }

        .canvas-container canvas,
        .canvas-container video {
            display: block;
            max-width: 100%;
            max-height: calc(100vh - 200px);
            border-radius: 12px;
        }

        #outputCanvas {
            position: absolute;
            top: 0;
            left: 0;
        }

        .upload-zone {
            width: 500px;
            height: 350px;
            border: 2px dashed var(--border);
            border-radius: 16px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 16px;
            cursor: pointer;
            transition: all 0.3s;
            background: var(--bg-secondary);
        }

        .upload-zone:hover {
            border-color: var(--accent);
            background: var(--accent-dim);
        }

        .upload-zone.dragover {
            border-color: var(--accent);
            background: var(--accent-dim);
            transform: scale(1.02);
        }

        .upload-icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: var(--bg-tertiary);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
        }

        .upload-text {
            font-size: 16px;
            font-weight: 500;
        }

        .upload-hint {
            font-size: 13px;
            color: var(--text-secondary);
        }

        /* Controls */
        .controls-bar {
            position: absolute;
            bottom: 24px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            gap: 12px;
            padding: 12px 16px;
            background: var(--bg-secondary);
            border-radius: 12px;
            border: 1px solid var(--border);
        }

        .control-btn {
            width: 44px;
            height: 44px;
            border-radius: 10px;
            background: var(--bg-tertiary);
            border: 1px solid var(--border);
            color: var(--text-primary);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            transition: all 0.2s;
        }

        .control-btn:hover {
            background: var(--border);
            transform: scale(1.05);
        }

        .control-btn.playing {
            background: var(--accent);
            color: var(--bg-primary);
            border-color: var(--accent);
        }

        /* Right Panel - Settings */
        .settings-panel {
            background: var(--bg-secondary);
            border-left: 1px solid var(--border);
            padding: 20px;
            overflow-y: auto;
        }

        .panel-title {
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .panel-title::before {
            content: '';
            width: 4px;
            height: 16px;
            background: var(--accent);
            border-radius: 2px;
        }

        .setting-group {
            margin-bottom: 24px;
        }

        .setting-label {
            font-size: 12px;
            font-weight: 500;
            color: var(--text-secondary);
            margin-bottom: 8px;
            display: flex;
            justify-content: space-between;
        }

        .setting-value {
            font-family: 'JetBrains Mono', monospace;
            color: var(--accent);
        }

        .slider {
            width: 100%;
            height: 6px;
            border-radius: 3px;
            background: var(--bg-tertiary);
            -webkit-appearance: none;
            appearance: none;
            cursor: pointer;
        }

        .slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            width: 16px;
            height: 16px;
            border-radius: 50%;
            background: var(--accent);
            cursor: pointer;
            transition: transform 0.2s;
        }

        .slider::-webkit-slider-thumb:hover {
            transform: scale(1.2);
        }

        .color-picker-group {
            display: flex;
            gap: 8px;
        }

        .color-swatch {
            width: 36px;
            height: 36px;
            border-radius: 8px;
            cursor: pointer;
            border: 2px solid transparent;
            transition: all 0.2s;
        }

        .color-swatch:hover,
        .color-swatch.active {
            border-color: var(--text-primary);
            transform: scale(1.1);
        }

        .toggle-group {
            display: flex;
            gap: 8px;
        }

        .toggle-btn {
            flex: 1;
            padding: 10px;
            border-radius: 8px;
            background: var(--bg-tertiary);
            border: 1px solid var(--border);
            color: var(--text-secondary);
            font-family: inherit;
            font-size: 12px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s;
        }

        .toggle-btn:hover {
            background: var(--border);
        }

        .toggle-btn.active {
            background: var(--accent-dim);
            border-color: var(--accent);
            color: var(--accent);
        }

        /* Progress */
        .progress-container {
            margin-top: 20px;
            padding: 16px;
            background: var(--bg-tertiary);
            border-radius: 12px;
        }

        .progress-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
            font-size: 13px;
        }

        .progress-bar {
            height: 4px;
            background: var(--border);
            border-radius: 2px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--accent), #00ccff);
            border-radius: 2px;
            transition: width 0.3s;
        }

        /* Hidden */
        .hidden {
            display: none !important;
        }

        /* Animations */
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        .processing .progress-fill {
            animation: pulse 1s ease-in-out infinite;
        }

        /* Responsive */
        @media (max-width: 1200px) {
            .main {
                grid-template-columns: 240px 1fr 280px;
            }
        }

        @media (max-width: 900px) {
            .main {
                grid-template-columns: 1fr;
                grid-template-rows: auto 1fr auto;
            }
            
            .sidebar, .settings-panel {
                border: none;
                border-bottom: 1px solid var(--border);
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="logo">
            <div class="logo-icon">MM</div>
            <div class="logo-text">Mixed<span>Media</span> Studio</div>
        </div>
        <div class="header-actions">
            <button class="btn btn-secondary" id="resetBtn">リセット</button>
            <button class="btn btn-primary" id="exportBtn" disabled>エクスポート</button>
        </div>
    </header>

    <main class="main">
        <!-- Left Sidebar - Effects -->
        <aside class="sidebar">
            <div class="sidebar-section">
                <div class="sidebar-title">スタイルエフェクト</div>
                <div class="effect-grid">
                    <div class="effect-card" data-effect="none">
                        <div class="effect-icon">◯</div>
                        <div class="effect-name">オリジナル</div>
                    </div>
                    <div class="effect-card" data-effect="sketch">
                        <div class="effect-icon">✏️</div>
                        <div class="effect-name">Sketch</div>
                    </div>
                    <div class="effect-card" data-effect="neon">
                        <div class="effect-icon">💡</div>
                        <div class="effect-name">Neon</div>
                    </div>
                    <div class="effect-card" data-effect="comic">
                        <div class="effect-icon">💥</div>
                        <div class="effect-name">Comic</div>
                    </div>
                    <div class="effect-card" data-effect="noir">
                        <div class="effect-icon">🎬</div>
                        <div class="effect-name">Noir</div>
                    </div>
                    <div class="effect-card" data-effect="glitch">
                        <div class="effect-icon">📺</div>
                        <div class="effect-name">Glitch</div>
                    </div>
                    <div class="effect-card" data-effect="pixel">
                        <div class="effect-icon">👾</div>
                        <div class="effect-name">Pixel</div>
                    </div>
                    <div class="effect-card" data-effect="vhs">
                        <div class="effect-icon">📼</div>
                        <div class="effect-name">VHS</div>
                    </div>
                </div>
            </div>

            <div class="sidebar-section">
                <div class="sidebar-title">オーバーレイ</div>
                <div class="effect-grid">
                    <div class="effect-card" data-overlay="none">
                        <div class="effect-icon">✕</div>
                        <div class="effect-name">なし</div>
                    </div>
                    <div class="effect-card" data-overlay="grain">
                        <div class="effect-icon">🌾</div>
                        <div class="effect-name">Grain</div>
                    </div>
                    <div class="effect-card" data-overlay="scanlines">
                        <div class="effect-icon">▤</div>
                        <div class="effect-name">Scanlines</div>
                    </div>
                    <div class="effect-card" data-overlay="vignette">
                        <div class="effect-icon">⬭</div>
                        <div class="effect-name">Vignette</div>
                    </div>
                </div>
            </div>
        </aside>

        <!-- Canvas Area -->
        <section class="canvas-area">
            <div class="upload-zone" id="uploadZone">
                <div class="upload-icon">🎬</div>
                <div class="upload-text">動画をドラッグ＆ドロップ</div>
                <div class="upload-hint">または クリックしてファイルを選択</div>
                <input type="file" id="fileInput" accept="video/*" style="display: none;">
            </div>

            <div class="canvas-container hidden" id="canvasContainer">
                <video id="sourceVideo" muted playsinline></video>
                <canvas id="outputCanvas"></canvas>
            </div>

            <div class="controls-bar hidden" id="controlsBar">
                <button class="control-btn" id="playPauseBtn">▶</button>
                <button class="control-btn" id="stopBtn">⏹</button>
                <button class="control-btn" id="loopBtn">🔁</button>
            </div>
        </section>

        <!-- Right Panel - Settings -->
        <aside class="settings-panel">
            <div class="panel-title">エフェクト設定</div>

            <div class="setting-group">
                <div class="setting-label">
                    <span>強度</span>
                    <span class="setting-value" id="intensityValue">100%</span>
                </div>
                <input type="range" class="slider" id="intensitySlider" min="0" max="100" value="100">
            </div>

            <div class="setting-group">
                <div class="setting-label">
                    <span>コントラスト</span>
                    <span class="setting-value" id="contrastValue">100%</span>
                </div>
                <input type="range" class="slider" id="contrastSlider" min="50" max="200" value="100">
            </div>

            <div class="setting-group">
                <div class="setting-label">
                    <span>明るさ</span>
                    <span class="setting-value" id="brightnessValue">100%</span>
                </div>
                <input type="range" class="slider" id="brightnessSlider" min="50" max="200" value="100">
            </div>

            <div class="setting-group">
                <div class="setting-label">
                    <span>彩度</span>
                    <span class="setting-value" id="saturationValue">100%</span>
                </div>
                <input type="range" class="slider" id="saturationSlider" min="0" max="200" value="100">
            </div>

            <div class="setting-group">
                <div class="setting-label">エッジカラー</div>
                <div class="color-picker-group">
                    <div class="color-swatch active" data-color="#00ff88" style="background: #00ff88;"></div>
                    <div class="color-swatch" data-color="#ff4466" style="background: #ff4466;"></div>
                    <div class="color-swatch" data-color="#00ccff" style="background: #00ccff;"></div>
                    <div class="color-swatch" data-color="#ffcc00" style="background: #ffcc00;"></div>
                    <div class="color-swatch" data-color="#ff00ff" style="background: #ff00ff;"></div>
                    <div class="color-swatch" data-color="#ffffff" style="background: #ffffff;"></div>
                </div>
            </div>

            <div class="setting-group">
                <div class="setting-label">ブレンドモード</div>
                <div class="toggle-group">
                    <button class="toggle-btn active" data-blend="normal">通常</button>
                    <button class="toggle-btn" data-blend="screen">スクリーン</button>
                    <button class="toggle-btn" data-blend="multiply">乗算</button>
                </div>
            </div>

            <div class="progress-container hidden" id="progressContainer">
                <div class="progress-header">
                    <span>処理中...</span>
                    <span id="progressPercent">0%</span>
                </div>
                <div class="progress-bar">
                    <div class="progress-fill" id="progressFill" style="width: 0%"></div>
                </div>
            </div>
        </aside>
    </main>

    <script>
        // State
        const state = {
            video: null,
            effect: 'none',
            overlay: 'none',
            intensity: 100,
            contrast: 100,
            brightness: 100,
            saturation: 100,
            edgeColor: '#00ff88',
            blendMode: 'normal',
            isPlaying: false,
            isLooping: true,
            animationId: null
        };

        // Elements
        const uploadZone = document.getElementById('uploadZone');
        const fileInput = document.getElementById('fileInput');
        const canvasContainer = document.getElementById('canvasContainer');
        const sourceVideo = document.getElementById('sourceVideo');
        const outputCanvas = document.getElementById('outputCanvas');
        const ctx = outputCanvas.getContext('2d', { willReadFrequently: true });
        const controlsBar = document.getElementById('controlsBar');
        const playPauseBtn = document.getElementById('playPauseBtn');
        const stopBtn = document.getElementById('stopBtn');
        const loopBtn = document.getElementById('loopBtn');
        const exportBtn = document.getElementById('exportBtn');
        const resetBtn = document.getElementById('resetBtn');

        // File Upload
        uploadZone.addEventListener('click', () => fileInput.click());
        uploadZone.addEventListener('dragover', (e) => {
            e.preventDefault();
            uploadZone.classList.add('dragover');
        });
        uploadZone.addEventListener('dragleave', () => {
            uploadZone.classList.remove('dragover');
        });
        uploadZone.addEventListener('drop', (e) => {
            e.preventDefault();
            uploadZone.classList.remove('dragover');
            const file = e.dataTransfer.files[0];
            if (file && file.type.startsWith('video/')) {
                loadVideo(file);
            }
        });
        fileInput.addEventListener('change', (e) => {
            const file = e.target.files[0];
            if (file) loadVideo(file);
        });

        function loadVideo(file) {
            const url = URL.createObjectURL(file);
            sourceVideo.src = url;
            sourceVideo.load();
            
            sourceVideo.onloadedmetadata = () => {
                outputCanvas.width = sourceVideo.videoWidth;
                outputCanvas.height = sourceVideo.videoHeight;
                
                uploadZone.classList.add('hidden');
                canvasContainer.classList.remove('hidden');
                controlsBar.classList.remove('hidden');
                exportBtn.disabled = false;
                
                state.video = sourceVideo;
                renderFrame();
            };
        }

        // Playback Controls
        playPauseBtn.addEventListener('click', togglePlayPause);
        stopBtn.addEventListener('click', stop);
        loopBtn.addEventListener('click', toggleLoop);

        function togglePlayPause() {
            if (state.isPlaying) {
                pause();
            } else {
                play();
            }
        }

        function play() {
            state.isPlaying = true;
            sourceVideo.play();
            playPauseBtn.textContent = '⏸';
            playPauseBtn.classList.add('playing');
            animate();
        }

        function pause() {
            state.isPlaying = false;
            sourceVideo.pause();
            playPauseBtn.textContent = '▶';
            playPauseBtn.classList.remove('playing');
            if (state.animationId) {
                cancelAnimationFrame(state.animationId);
            }
        }

        function stop() {
            pause();
            sourceVideo.currentTime = 0;
            renderFrame();
        }

        function toggleLoop() {
            state.isLooping = !state.isLooping;
            sourceVideo.loop = state.isLooping;
            loopBtn.style.opacity = state.isLooping ? 1 : 0.5;
        }

        sourceVideo.addEventListener('ended', () => {
            if (!state.isLooping) {
                pause();
            }
        });

        // Animation Loop
        function animate() {
            if (!state.isPlaying) return;
            renderFrame();
            state.animationId = requestAnimationFrame(animate);
        }

        // Effects Processing
        function renderFrame() {
            if (!state.video) return;

            const w = outputCanvas.width;
            const h = outputCanvas.height;

            // Draw original frame
            ctx.drawImage(sourceVideo, 0, 0, w, h);

            // Get image data
            let imageData = ctx.getImageData(0, 0, w, h);
            let data = imageData.data;

            // Apply base adjustments
            applyAdjustments(data);

            // Apply effect
            if (state.effect !== 'none' && state.intensity > 0) {
                const effectData = ctx.createImageData(w, h);
                const effectPixels = effectData.data;
                
                switch (state.effect) {
                    case 'sketch':
                        applySketchEffect(data, effectPixels, w, h);
                        break;
                    case 'neon':
                        applyNeonEffect(data, effectPixels, w, h);
                        break;
                    case 'comic':
                        applyComicEffect(data, effectPixels, w, h);
                        break;
                    case 'noir':
                        applyNoirEffect(data, effectPixels, w, h);
                        break;
                    case 'glitch':
                        applyGlitchEffect(data, effectPixels, w, h);
                        break;
                    case 'pixel':
                        applyPixelEffect(data, effectPixels, w, h);
                        break;
                    case 'vhs':
                        applyVHSEffect(data, effectPixels, w, h);
                        break;
                    default:
                        effectPixels.set(data);
                }

                // Blend with intensity
                const intensity = state.intensity / 100;
                for (let i = 0; i < data.length; i += 4) {
                    data[i] = data[i] * (1 - intensity) + effectPixels[i] * intensity;
                    data[i + 1] = data[i + 1] * (1 - intensity) + effectPixels[i + 1] * intensity;
                    data[i + 2] = data[i + 2] * (1 - intensity) + effectPixels[i + 2] * intensity;
                }
            }

            ctx.putImageData(imageData, 0, 0);

            // Apply overlays
            applyOverlay(w, h);
        }

        function applyAdjustments(data) {
            const contrast = state.contrast / 100;
            const brightness = state.brightness / 100;
            const saturation = state.saturation / 100;

            for (let i = 0; i < data.length; i += 4) {
                let r = data[i];
                let g = data[i + 1];
                let b = data[i + 2];

                // Brightness
                r *= brightness;
                g *= brightness;
                b *= brightness;

                // Contrast
                r = ((r / 255 - 0.5) * contrast + 0.5) * 255;
                g = ((g / 255 - 0.5) * contrast + 0.5) * 255;
                b = ((b / 255 - 0.5) * contrast + 0.5) * 255;

                // Saturation
                const gray = 0.299 * r + 0.587 * g + 0.114 * b;
                r = gray + (r - gray) * saturation;
                g = gray + (g - gray) * saturation;
                b = gray + (b - gray) * saturation;

                data[i] = Math.max(0, Math.min(255, r));
                data[i + 1] = Math.max(0, Math.min(255, g));
                data[i + 2] = Math.max(0, Math.min(255, b));
            }
        }

        // Effect Functions
        function applySketchEffect(src, dst, w, h) {
            // Sobel edge detection
            const sobelX = [-1, 0, 1, -2, 0, 2, -1, 0, 1];
            const sobelY = [-1, -2, -1, 0, 0, 0, 1, 2, 1];

            for (let y = 1; y < h - 1; y++) {
                for (let x = 1; x < w - 1; x++) {
                    let gx = 0, gy = 0;
                    
                    for (let ky = -1; ky <= 1; ky++) {
                        for (let kx = -1; kx <= 1; kx++) {
                            const idx = ((y + ky) * w + (x + kx)) * 4;
                            const gray = (src[idx] + src[idx + 1] + src[idx + 2]) / 3;
                            const ki = (ky + 1) * 3 + (kx + 1);
                            gx += gray * sobelX[ki];
                            gy += gray * sobelY[ki];
                        }
                    }
                    
                    const edge = Math.sqrt(gx * gx + gy * gy);
                    const val = 255 - Math.min(255, edge);
                    
                    const i = (y * w + x) * 4;
                    dst[i] = val;
                    dst[i + 1] = val;
                    dst[i + 2] = val;
                    dst[i + 3] = 255;
                }
            }
        }

        function applyNeonEffect(src, dst, w, h) {
            const color = hexToRgb(state.edgeColor);
            const sobelX = [-1, 0, 1, -2, 0, 2, -1, 0, 1];
            const sobelY = [-1, -2, -1, 0, 0, 0, 1, 2, 1];

            for (let y = 1; y < h - 1; y++) {
                for (let x = 1; x < w - 1; x++) {
                    let gx = 0, gy = 0;
                    
                    for (let ky = -1; ky <= 1; ky++) {
                        for (let kx = -1; kx <= 1; kx++) {
                            const idx = ((y + ky) * w + (x + kx)) * 4;
                            const gray = (src[idx] + src[idx + 1] + src[idx + 2]) / 3;
                            const ki = (ky + 1) * 3 + (kx + 1);
                            gx += gray * sobelX[ki];
                            gy += gray * sobelY[ki];
                        }
                    }
                    
                    const edge = Math.min(1, Math.sqrt(gx * gx + gy * gy) / 200);
                    const glow = Math.pow(edge, 0.5);
                    
                    const i = (y * w + x) * 4;
                    dst[i] = glow * color.r;
                    dst[i + 1] = glow * color.g;
                    dst[i + 2] = glow * color.b;
                    dst[i + 3] = 255;
                }
            }
        }

        function applyComicEffect(src, dst, w, h) {
            // Posterize + edge detection
            const levels = 5;
            
            for (let i = 0; i < src.length; i += 4) {
                let r = Math.floor(src[i] / (256 / levels)) * (256 / levels);
                let g = Math.floor(src[i + 1] / (256 / levels)) * (256 / levels);
                let b = Math.floor(src[i + 2] / (256 / levels)) * (256 / levels);
                
                // Boost saturation
                const gray = 0.299 * r + 0.587 * g + 0.114 * b;
                r = gray + (r - gray) * 1.5;
                g = gray + (g - gray) * 1.5;
                b = gray + (b - gray) * 1.5;
                
                dst[i] = Math.max(0, Math.min(255, r));
                dst[i + 1] = Math.max(0, Math.min(255, g));
                dst[i + 2] = Math.max(0, Math.min(255, b));
                dst[i + 3] = 255;
            }
        }

        function applyNoirEffect(src, dst, w, h) {
            for (let i = 0; i < src.length; i += 4) {
                const gray = 0.299 * src[i] + 0.587 * src[i + 1] + 0.114 * src[i + 2];
                // High contrast B&W
                const val = ((gray / 255 - 0.5) * 1.8 + 0.5) * 255;
                const clamped = Math.max(0, Math.min(255, val));
                
                dst[i] = clamped;
                dst[i + 1] = clamped;
                dst[i + 2] = clamped;
                dst[i + 3] = 255;
            }
        }

        function applyGlitchEffect(src, dst, w, h) {
            const time = Date.now() / 100;
            const sliceCount = 10 + Math.floor(Math.random() * 10);
            
            dst.set(src);
            
            for (let s = 0; s < sliceCount; s++) {
                if (Math.random() > 0.7) {
                    const y = Math.floor(Math.random() * h);
                    const sliceH = Math.floor(Math.random() * 20) + 5;
                    const offset = Math.floor((Math.random() - 0.5) * 40);
                    
                    for (let dy = 0; dy < sliceH && y + dy < h; dy++) {
                        for (let x = 0; x < w; x++) {
                            const srcX = Math.max(0, Math.min(w - 1, x + offset));
                            const srcI = ((y + dy) * w + srcX) * 4;
                            const dstI = ((y + dy) * w + x) * 4;
                            
                            // RGB shift
                            dst[dstI] = src[srcI + (Math.random() > 0.5 ? 4 : 0)] || src[srcI];
                            dst[dstI + 1] = src[srcI + 1];
                            dst[dstI + 2] = src[Math.max(0, srcI - 4) + 2] || src[srcI + 2];
                            dst[dstI + 3] = 255;
                        }
                    }
                }
            }
        }

        function applyPixelEffect(src, dst, w, h) {
            const pixelSize = 8;
            
            for (let y = 0; y < h; y += pixelSize) {
                for (let x = 0; x < w; x += pixelSize) {
                    let r = 0, g = 0, b = 0, count = 0;
                    
                    for (let py = 0; py < pixelSize && y + py < h; py++) {
                        for (let px = 0; px < pixelSize && x + px < w; px++) {
                            const i = ((y + py) * w + (x + px)) * 4;
                            r += src[i];
                            g += src[i + 1];
                            b += src[i + 2];
                            count++;
                        }
                    }
                    
                    r = Math.floor(r / count);
                    g = Math.floor(g / count);
                    b = Math.floor(b / count);
                    
                    for (let py = 0; py < pixelSize && y + py < h; py++) {
                        for (let px = 0; px < pixelSize && x + px < w; px++) {
                            const i = ((y + py) * w + (x + px)) * 4;
                            dst[i] = r;
                            dst[i + 1] = g;
                            dst[i + 2] = b;
                            dst[i + 3] = 255;
                        }
                    }
                }
            }
        }

        function applyVHSEffect(src, dst, w, h) {
            const time = Date.now() / 50;
            
            for (let y = 0; y < h; y++) {
                const wobble = Math.sin(y * 0.1 + time) * 2;
                
                for (let x = 0; x < w; x++) {
                    const srcX = Math.max(0, Math.min(w - 1, Math.floor(x + wobble)));
                    const i = (y * w + x) * 4;
                    const si = (y * w + srcX) * 4;
                    
                    // Chromatic aberration
                    dst[i] = src[Math.max(0, si - 8)] || src[si];
                    dst[i + 1] = src[si + 1];
                    dst[i + 2] = src[Math.min(src.length - 4, si + 8) + 2] || src[si + 2];
                    dst[i + 3] = 255;
                    
                    // Add noise
                    if (Math.random() > 0.97) {
                        const noise = Math.random() * 50;
                        dst[i] += noise;
                        dst[i + 1] += noise;
                        dst[i + 2] += noise;
                    }
                }
            }
        }

        function applyOverlay(w, h) {
            ctx.globalCompositeOperation = state.blendMode === 'screen' ? 'screen' : 
                                           state.blendMode === 'multiply' ? 'multiply' : 'source-over';

            switch (state.overlay) {
                case 'grain':
                    applyGrainOverlay(w, h);
                    break;
                case 'scanlines':
                    applyScanlinesOverlay(w, h);
                    break;
                case 'vignette':
                    applyVignetteOverlay(w, h);
                    break;
            }

            ctx.globalCompositeOperation = 'source-over';
        }

        function applyGrainOverlay(w, h) {
            const imageData = ctx.getImageData(0, 0, w, h);
            const data = imageData.data;
            
            for (let i = 0; i < data.length; i += 4) {
                const noise = (Math.random() - 0.5) * 30;
                data[i] += noise;
                data[i + 1] += noise;
                data[i + 2] += noise;
            }
            
            ctx.putImageData(imageData, 0, 0);
        }

        function applyScanlinesOverlay(w, h) {
            ctx.fillStyle = 'rgba(0, 0, 0, 0.15)';
            for (let y = 0; y < h; y += 4) {
                ctx.fillRect(0, y, w, 2);
            }
        }

        function applyVignetteOverlay(w, h) {
            const gradient = ctx.createRadialGradient(w/2, h/2, 0, w/2, h/2, Math.max(w, h) * 0.7);
            gradient.addColorStop(0, 'rgba(0,0,0,0)');
            gradient.addColorStop(1, 'rgba(0,0,0,0.7)');
            ctx.fillStyle = gradient;
            ctx.fillRect(0, 0, w, h);
        }

        // Utility
        function hexToRgb(hex) {
            const result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
            return result ? {
                r: parseInt(result[1], 16),
                g: parseInt(result[2], 16),
                b: parseInt(result[3], 16)
            } : { r: 0, g: 255, b: 136 };
        }

        // Event Listeners - Effects
        document.querySelectorAll('.effect-card[data-effect]').forEach(card => {
            card.addEventListener('click', () => {
                document.querySelectorAll('.effect-card[data-effect]').forEach(c => c.classList.remove('active'));
                card.classList.add('active');
                state.effect = card.dataset.effect;
                renderFrame();
            });
        });

        document.querySelectorAll('.effect-card[data-overlay]').forEach(card => {
            card.addEventListener('click', () => {
                document.querySelectorAll('.effect-card[data-overlay]').forEach(c => c.classList.remove('active'));
                card.classList.add('active');
                state.overlay = card.dataset.overlay;
                renderFrame();
            });
        });

        // Event Listeners - Sliders
        const sliders = [
            { id: 'intensitySlider', prop: 'intensity', display: 'intensityValue' },
            { id: 'contrastSlider', prop: 'contrast', display: 'contrastValue' },
            { id: 'brightnessSlider', prop: 'brightness', display: 'brightnessValue' },
            { id: 'saturationSlider', prop: 'saturation', display: 'saturationValue' }
        ];

        sliders.forEach(({ id, prop, display }) => {
            const slider = document.getElementById(id);
            slider.addEventListener('input', () => {
                state[prop] = parseInt(slider.value);
                document.getElementById(display).textContent = slider.value + '%';
                renderFrame();
            });
        });

        // Color picker
        document.querySelectorAll('.color-swatch').forEach(swatch => {
            swatch.addEventListener('click', () => {
                document.querySelectorAll('.color-swatch').forEach(s => s.classList.remove('active'));
                swatch.classList.add('active');
                state.edgeColor = swatch.dataset.color;
                renderFrame();
            });
        });

        // Blend mode
        document.querySelectorAll('.toggle-btn[data-blend]').forEach(btn => {
            btn.addEventListener('click', () => {
                document.querySelectorAll('.toggle-btn[data-blend]').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                state.blendMode = btn.dataset.blend;
                renderFrame();
            });
        });

        // Reset
        resetBtn.addEventListener('click', () => {
            location.reload();
        });

        // Export
        exportBtn.addEventListener('click', async () => {
            if (!state.video) return;
            
            const progressContainer = document.getElementById('progressContainer');
            const progressFill = document.getElementById('progressFill');
            const progressPercent = document.getElementById('progressPercent');
            
            progressContainer.classList.remove('hidden');
            progressContainer.classList.add('processing');
            
            const wasPlaying = state.isPlaying;
            if (wasPlaying) pause();
            
            const stream = outputCanvas.captureStream(30);
            const mediaRecorder = new MediaRecorder(stream, { 
                mimeType: 'video/webm;codecs=vp9',
                videoBitsPerSecond: 8000000
            });
            
            const chunks = [];
            mediaRecorder.ondataavailable = (e) => chunks.push(e.data);
            
            mediaRecorder.onstop = () => {
                const blob = new Blob(chunks, { type: 'video/webm' });
                const url = URL.createObjectURL(blob);
                const a = document.createElement('a');
                a.href = url;
                a.download = 'mixed-media-export.webm';
                a.click();
                URL.revokeObjectURL(url);
                
                progressContainer.classList.add('hidden');
                progressContainer.classList.remove('processing');
            };
            
            mediaRecorder.start();
            sourceVideo.currentTime = 0;
            
            const duration = sourceVideo.duration;
            
            const exportLoop = () => {
                if (sourceVideo.currentTime >= duration - 0.1) {
                    mediaRecorder.stop();
                    return;
                }
                
                renderFrame();
                const progress = (sourceVideo.currentTime / duration) * 100;
                progressFill.style.width = progress + '%';
                progressPercent.textContent = Math.floor(progress) + '%';
                
                requestAnimationFrame(exportLoop);
            };
            
            sourceVideo.play();
            exportLoop();
        });

        // Initialize default selections
        document.querySelector('.effect-card[data-effect="none"]').classList.add('active');
        document.querySelector('.effect-card[data-overlay="none"]').classList.add('active');
        sourceVideo.loop = state.isLooping;
    </script>
</body>
</html>
